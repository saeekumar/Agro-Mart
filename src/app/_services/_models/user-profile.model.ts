export interface UserProfile {
    id: any;
    name: string;
    email: string;
    gender: string;
    phone: any;
    address: string;
    pincode: string;
    // profilePictureUrl: string; // Optional, if you have this field
}